<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IT13_FinalProject.ServiceManagementView">
    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="24">
            <Label Text="Service Management"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="#1e2445"
                   HorizontalOptions="Center" />

            <!-- Search Bar + Add Button -->
            <Frame BackgroundColor="#4B91FF"
                   CornerRadius="16"
                   Padding="18"
                   Margin="0,0,0,12">
                <Grid RowSpacing="12" ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Entry Placeholder="Search services..."
                           PlaceholderColor="LightGray"
                           TextColor="White"
                           TextChanged="OnSearchTextChanged"
                           Grid.Column="0" />

                    <Button Text="Add Service"
                            Grid.Column="1"
                            BackgroundColor="#FFB84B"
                            TextColor="White"
                            CornerRadius="8"
                            Command="{Binding AddServiceCommand}" />
                </Grid>
            </Frame>

            <!-- Service List -->
            <CollectionView ItemsSource="{Binding FilteredServices}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="24" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="#2D3748"
                               CornerRadius="16"
                               Padding="18"
                               Margin="0,0,0,24">
                            <VerticalStackLayout Spacing="8">
                                <Label Text="{Binding Name}"
                                       FontSize="20"
                                       FontAttributes="Bold"
                                       TextColor="White" />
                                <Label Text="Price: {Binding Price, StringFormat='${0:F2}'}"
                                       FontSize="14"
                                       TextColor="#FFB84B" />
                                <Label Text="Duration: {Binding Duration}"
                                       FontSize="14"
                                       TextColor="WhiteSmoke" />
                                <Label Text="Description: {Binding Description}"
                                       FontSize="14"
                                       TextColor="#ccc" />

                                <!-- Action Buttons -->
                                <HorizontalStackLayout Spacing="12">
                                    <Button Text="View"
                                            BackgroundColor="#2D3748"
                                            TextColor="White"
                                            CornerRadius="8"
                                            HeightRequest="44"
                                            FontSize="16"
                                            Padding="16,0"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentView}}, Path=BindingContext.ViewServiceCommand}"
                                            CommandParameter="{Binding .}" />
                                    <Button Text="Edit"
                                            BackgroundColor="#4B91FF"
                                            TextColor="White"
                                            CornerRadius="8"
                                            HeightRequest="44"
                                            FontSize="16"
                                            Padding="16,0"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentView}}, Path=BindingContext.EditServiceCommand}"
                                            CommandParameter="{Binding .}" />
                                    <Button Text="Delete"
                                            BackgroundColor="#FF4B91"
                                            TextColor="White"
                                            CornerRadius="8"
                                            HeightRequest="44"
                                            FontSize="16"
                                            Padding="16,0"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentView}}, Path=BindingContext.DeleteServiceCommand}"
                                            CommandParameter="{Binding .}" />
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentView>