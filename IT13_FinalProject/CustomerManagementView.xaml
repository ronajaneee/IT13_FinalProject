<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IT13_FinalProject.CustomerManagementView">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">
            <Label Text="Customer Management"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="#1e2445"
                   HorizontalOptions="Center"
                   Margin="0,0,0,10" />

            <Button Text="Add Customer"
                    BackgroundColor="#FF4B91"
                    TextColor="White"
                    CornerRadius="8"
                    HeightRequest="44"
                    HorizontalOptions="End"
                    Margin="0,0,0,16"
                    Clicked="OnAddCustomerClicked" />

            <CollectionView ItemsSource="{Binding Customers}">
                <CollectionView.ItemsLayout>
                    <!-- Moderate spacing between customer cards -->
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="24" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="#2D3748"
                               CornerRadius="16"
                               Padding="16"
                               Margin="0,0,0,24"
                               HasShadow="False">
                            <VerticalStackLayout Spacing="12">
                                <Label Text="{Binding Name}"
                                       FontSize="20"
                                       FontAttributes="Bold"
                                       TextColor="White" />
                                <Label Text="Contact: {Binding ContactDetails}"
                                       FontSize="14"
                                       TextColor="WhiteSmoke" />
                                <Label Text="Loyalty Points: {Binding LoyaltyPoints}"
                                       FontSize="14"
                                       TextColor="#FFB84B" />
                                <Label Text="Booking History: {Binding BookingHistory}"
                                       FontSize="14"
                                       TextColor="#ccc" />

                                <!-- Buttons with moderate spacing between columns -->
                                <Grid ColumnSpacing="16" Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="0"
                                            Text="Edit"
                                            BackgroundColor="#FFB84B"
                                            TextColor="#1e2445"
                                            CornerRadius="8"
                                            Clicked="OnEditCustomerClicked" />
                                    <Button Grid.Column="1"
                                            Text="Delete"
                                            BackgroundColor="#FF4B91"
                                            TextColor="White"
                                            CornerRadius="8"
                                            Clicked="OnDeleteCustomerClicked" />
                                    <Button Grid.Column="2"
                                            Text="History"
                                            BackgroundColor="#2D3748"
                                            TextColor="#FF4B91"
                                            CornerRadius="8"
                                            Clicked="OnViewHistoryClicked" />
                                </Grid>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentView>