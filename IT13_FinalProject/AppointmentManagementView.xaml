<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IT13_FinalProject.AppointmentManagementView">
    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="24">
            <Label Text="Appointment Management"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="#1e2445"
                   HorizontalOptions="Center" />

            <!-- Search Bar + Add Button -->
            <Frame BackgroundColor="#4B91FF"
                   CornerRadius="16"
                   Padding="18"
                   Margin="0,0,0,12">
                <Grid RowSpacing="12" ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Entry Placeholder="Search appointments..."
                           PlaceholderColor="LightGray"
                           TextColor="White"
                           TextChanged="OnSearchTextChanged"
                           Grid.Column="0" />

                    <Button Text="Add Appointment"
                            Grid.Column="1"
                            BackgroundColor="#FFB84B"
                            TextColor="White"
                            CornerRadius="8"
                            Clicked="OnAddAppointmentClicked" />
                </Grid>
            </Frame>

            <!-- Appointment Summary List -->
            <CollectionView ItemsSource="{Binding FilteredAppointments}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="16" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="#2D3748"
                               CornerRadius="12"
                               Padding="14"
                               Margin="0,0,0,12">
                            <VerticalStackLayout Spacing="8">
                                <Grid RowSpacing="6" ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Label Grid.Row="0" Grid.Column="0" Text="ID:" FontAttributes="Bold" TextColor="#FFB84B" />
                                    <Label Grid.Row="0" Grid.Column="1" Text="{Binding AppointmentId}" TextColor="White" />
                                    <Label Grid.Row="1" Grid.Column="0" Text="Customer:" FontAttributes="Bold" TextColor="#FFB84B" />
                                    <Label Grid.Row="1" Grid.Column="1" Text="{Binding CustomerName}" TextColor="White" />
                                    <Label Grid.Row="2" Grid.Column="0" Text="Date:" FontAttributes="Bold" TextColor="#FFB84B" />
                                    <Label Grid.Row="2" Grid.Column="1" Text="{Binding AppointmentDateTimeString}" TextColor="WhiteSmoke" />
                                </Grid>
                                <HorizontalStackLayout Spacing="12" Margin="0,10,0,0">
                                    <Button Text="View Details"
                                            BackgroundColor="#2D3748"
                                            TextColor="White"
                                            CornerRadius="8"
                                            HeightRequest="44"
                                            FontSize="16"
                                            Padding="16,0"
                                            Clicked="OnViewDetailsClicked" />
                                    <Button Text="Edit"
                                            BackgroundColor="#4B91FF"
                                            TextColor="White"
                                            CornerRadius="8"
                                            HeightRequest="44"
                                            FontSize="16"
                                            Padding="16,0"
                                            Clicked="OnEditAppointmentClicked" />
                                    <Button Text="Delete"
                                            BackgroundColor="#FF4B91"
                                            TextColor="White"
                                            CornerRadius="8"
                                            HeightRequest="44"
                                            FontSize="16"
                                            Padding="16,0"
                                            Clicked="OnDeleteAppointmentClicked" />
                                    <Button Text="Status"
                                            BackgroundColor="#4BCB4B"
                                            TextColor="#1e2445"
                                            CornerRadius="8"
                                            HeightRequest="44"
                                            FontSize="16"
                                            Padding="16,0"
                                            Clicked="OnStatusUpdateClicked" />
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentView>